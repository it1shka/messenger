import styled from 'styled-components'

const DialogContainer = styled.div`
  grid-row: 2 / 3;
  grid-column: 2 / 3;
`

const DialogView = () => {
  return (
    <DialogContainer>

    </DialogContainer>
  )
}

export default DialogView